const SUPABASE_URL='https://lrbimrlbskjweynxlgas.supabase.co';
const SUPABASE_PUBLISHABLE_KEY='sb_publishable_RlmqwQM8ATOc7-ML9hvwgw_UljUEavh';
const STRIPE_PUBLISHABLE_KEY='';
const client=window.supabase.createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
const $=id=>document.getElementById(id);
const money=v=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(v)||0);
const date=v=>v?new Date(`${v}T12:00:00`).toLocaleDateString(undefined,{month:'long',day:'numeric',year:'numeric'}):'—';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let token='',invoice=null,stripe=null,elements=null,paymentElement=null,clientSecret='',paymentIntentId='',paymentMode='full',paymentAmount=0;
function showOnly(id){['checkoutLoading','checkoutError','checkoutPaid','checkoutPartial','checkoutApp'].forEach(x=>$(x).hidden=x!==id)}
function showError(message){$('checkoutErrorMessage').textContent=message||'Unable to load this invoice.';showOnly('checkoutError')}
function showPaymentMessage(message){$('paymentMessage').textContent=message;$('paymentMessage').hidden=false}
function clearPaymentMessage(){$('paymentMessage').hidden=true;$('paymentMessage').textContent=''}
async function invoke(action,extra={}){const {data,error}=await client.functions.invoke('invoice-checkout',{body:{action,token,...extra}});if(error)throw error;if(data?.error)throw new Error(data.error);return data}
function balance(){return Math.max(Number(invoice?.balance_due??invoice?.total_amount??0),0)}
function minimumPartial(){return Math.min(balance(),Number(invoice?.minimum_partial_amount??Math.ceil(balance()*25)/100))}
function renderInvoice(){
 $('invoiceNumber').textContent=invoice.invoice_number||'Invoice';$('dueDate').textContent=date(invoice.due_date);$('customerEmail').textContent=invoice.client_email||'—';$('paymentTerms').textContent=invoice.payment_terms||'Due upon receipt';$('paymentAmount').textContent=money(balance());
 const lines=(invoice.invoice_line_items||[]).sort((a,b)=>Number(a.line_number)-Number(b.line_number));$('invoiceLines').innerHTML=lines.map(l=>`<div class="line-row"><span>${esc(l.description)}</span><span class="qty">${Number(l.quantity)||0}</span><span class="amount">${money(l.line_total)}</span></div>`).join('')||`<div class="line-row"><span>filings4u services</span><span class="qty">1</span><span class="amount">${money(invoice.subtotal_amount)}</span></div>`;
 const discountLabel=invoice.discount_type==='percent'&&Number(invoice.discount_value)?`Discount (${Number(invoice.discount_value)}%)`:'Discount';$('invoiceTotals').innerHTML=`<div class="total-row"><span>Subtotal</span><strong>${money(invoice.subtotal_amount)}</strong></div>${Number(invoice.discount_amount)>0?`<div class="total-row"><span>${discountLabel}</span><strong>− ${money(invoice.discount_amount)}</strong></div>`:''}${Number(invoice.tax_amount)>0?`<div class="total-row"><span>Tax${Number(invoice.tax_rate)?` (${Number(invoice.tax_rate)}%)`:''}</span><strong>${money(invoice.tax_amount)}</strong></div>`:''}${Number(invoice.shipping_amount)>0?`<div class="total-row"><span>Shipping</span><strong>${money(invoice.shipping_amount)}</strong></div>`:''}<div class="total-row"><span>Invoice total</span><strong>${money(invoice.total_amount)}</strong></div>${Number(invoice.amount_paid)>0?`<div class="total-row paid-row"><span>Paid to date</span><strong>− ${money(invoice.amount_paid)}</strong></div>`:''}<div class="total-row grand"><span>Balance due</span><strong>${money(balance())}</strong></div>`;
 if(invoice.customer_notes){$('customerNotes').textContent=invoice.customer_notes;$('customerNotesWrap').hidden=false}
 $('partialMinimum').textContent=money(minimumPartial());$('partialAmount').min=minimumPartial().toFixed(2);$('partialAmount').max=balance().toFixed(2);$('partialAmount').value=minimumPartial().toFixed(2);
}
function destroyPayment(){if(paymentElement){try{paymentElement.destroy()}catch(e){try{paymentElement.unmount()}catch(_){}}}paymentElement=null;elements=null;clientSecret='';paymentIntentId='';$('paymentElement').innerHTML=''}
async function preparePayment(mode='full',amount=null){destroyPayment();clearPaymentMessage();paymentMode=mode;paymentAmount=mode==='full'?balance():Number(amount);$('payButton').disabled=true;$('payButtonText').textContent='Preparing payment…';const data=await invoke('create_payment',{payment_mode:mode,amount:paymentAmount});if(data.paid)return showPaid(data.invoice_number||invoice.invoice_number);clientSecret=data.client_secret;paymentIntentId=data.payment_intent_id;paymentAmount=Number(data.payment_amount||paymentAmount);const publishableKey=data.publishable_key||STRIPE_PUBLISHABLE_KEY;if(!publishableKey)throw new Error('Stripe publishable key is not configured in Edge Function secrets.');stripe=Stripe(publishableKey);elements=stripe.elements({clientSecret,appearance:{theme:'stripe',variables:{colorPrimary:'#10b981',borderRadius:'9px',fontFamily:'DM Sans, sans-serif'}}});paymentElement=elements.create('payment',{layout:'tabs'});paymentElement.mount('#paymentElement');$('payButton').disabled=false;$('payButtonText').textContent=`Pay ${money(paymentAmount)}`}
function setPaymentOptions(open){$('partialOptions').hidden=!open;$('paymentOptionsLink').setAttribute('aria-expanded',String(open));$('paymentOptionsLink').textContent=open?'Pay in full instead':'Payment options'}
async function verifyAndFinish(){const result=await invoke('verify_payment',{payment_intent_id:paymentIntentId});if(result.paid){showPaid(result.invoice_number);return true}if(result.partial){invoice.amount_paid=result.amount_paid;invoice.balance_due=result.balance_due;showPartial(result);return true}return false}
function showPaid(number){destroyPayment();$('paidInvoiceNumber').textContent=number||invoice?.invoice_number||'Invoice';showOnly('checkoutPaid')}
function showPartial(result){destroyPayment();$('partialPaidAmount').textContent=money(result.payment_amount);$('partialRemainingBalance').textContent=money(result.balance_due);$('partialInvoiceNumber').textContent=result.invoice_number||invoice?.invoice_number||'Invoice';showOnly('checkoutPartial')}
async function submitPayment(event){event.preventDefault();clearPaymentMessage();if(!stripe||!elements||!clientSecret)return;const button=$('payButton');button.disabled=true;$('payButtonText').textContent='Processing payment…';try{const {error:submitError}=await elements.submit();if(submitError)throw submitError;const {error,paymentIntent}=await stripe.confirmPayment({elements,clientSecret,redirect:'if_required',confirmParams:{return_url:location.href}});if(error)throw error;if(paymentIntent?.status==='succeeded'){paymentIntentId=paymentIntent.id;const completed=await verifyAndFinish();if(!completed)throw new Error('Payment was submitted, but confirmation is still processing. Refresh this page in a moment.');return}throw new Error(`Payment status: ${paymentIntent?.status||'pending'}.`)}catch(error){showPaymentMessage(error.message||'Payment could not be completed.');button.disabled=false;$('payButtonText').textContent=`Pay ${money(paymentAmount)}`}}
async function boot(){token=new URLSearchParams(location.search).get('invoice')||'';if(!token)return showError('This invoice payment link is missing its secure token.');try{const data=await invoke('load');invoice=data.invoice;if(!invoice)throw new Error('Invoice not found.');if(invoice.payment_status==='paid'||balance()<=0)return showPaid(invoice.invoice_number);renderInvoice();showOnly('checkoutApp');setPaymentOptions(false);await preparePayment('full')}catch(error){showError(error.message||'Unable to load this invoice.')}}
$('paymentForm').addEventListener('submit',submitPayment);
$('paymentOptionsLink').addEventListener('click',async()=>{const open=$('partialOptions').hidden;setPaymentOptions(open);try{if(open){await preparePayment('partial',Number($('partialAmount').value))}else{await preparePayment('full')}}catch(e){showPaymentMessage(e.message)}});
$('applyPartial').addEventListener('click',async()=>{const amount=Number($('partialAmount').value);if(amount<minimumPartial()){showPaymentMessage(`Partial payments must be at least ${money(minimumPartial())}.`);return}if(amount>balance()){showPaymentMessage('Partial payment cannot exceed the remaining balance.');return}try{await preparePayment('partial',amount)}catch(e){showPaymentMessage(e.message)}});
$('continuePaying').addEventListener('click',()=>location.reload());
boot();
